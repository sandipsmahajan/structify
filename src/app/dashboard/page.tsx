import { BookOpen, Flame, Medal, Star, Target } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { Card, Kpi, SectionTitle } from "@/components/ui";
import { achievements, roadmap } from "@/lib/content";

export default function DashboardPage() {
  return <AppShell><div className="mx-auto max-w-7xl px-4 py-10"><SectionTitle eyebrow="dashboard" title="Continue learning" text="Track XP, streaks, achievements, bookmarks, and course progress." /><div className="mt-8 grid gap-4 md:grid-cols-4"><Kpi label="XP" value="8,420" /><Kpi label="Daily streak" value="14" tone="amber" /><Kpi label="Coins" value="1,260" tone="mint" /><Kpi label="Completed" value="28%" tone="coral" /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.4fr_0.8fr]"><Card><h2 className="font-display text-2xl font-extrabold">Learning queue</h2><div className="mt-4 space-y-3">{roadmap.slice(0, 5).map((course) => <div key={course.id} className="rounded-lg bg-slate-100 p-4 dark:bg-white/10"><div className="flex items-center justify-between"><span className="font-bold">{course.title}</span><span className="text-sm">{course.progress}%</span></div><div className="mt-3 h-2 rounded-full bg-white dark:bg-ink"><div className="h-2 rounded-full bg-brand" style={{ width: `${course.progress}%` }} /></div></div>)}</div></Card><Card><h2 className="font-display text-2xl font-extrabold">Achievements</h2><div className="mt-4 grid gap-3">{achievements.map((item, index) => { const icons = [Flame, Star, Medal, Target, BookOpen]; const Icon = icons[index]; return <div key={item} className="flex items-center gap-3 rounded-lg bg-slate-100 p-3 dark:bg-white/10"><Icon className="text-brand" size={20} /><span className="font-semibold">{item}</span></div>; })}</div></Card></div></div></AppShell>;
}
