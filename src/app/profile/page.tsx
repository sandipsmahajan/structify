import { Award, Bookmark, CalendarDays, Gauge, Settings } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { Card, Kpi, SectionTitle } from "@/components/ui";
import { achievements } from "@/lib/content";

export default function ProfilePage() {
  return <AppShell><div className="mx-auto max-w-7xl px-4 py-10"><SectionTitle eyebrow="profile" title="Ada Learner" text="Personal progress, accessibility preferences, bookmarks, achievements, and certificates." /><div className="mt-8 grid gap-4 md:grid-cols-4"><Kpi label="Level" value="18" /><Kpi label="XP" value="8,420" tone="mint" /><Kpi label="Streak" value="14" tone="amber" /><Kpi label="Rank" value="#412" tone="coral" /></div><div className="mt-8 grid gap-6 lg:grid-cols-3"><Card><Settings className="text-brand" /><h2 className="mt-3 font-display text-2xl font-extrabold">Accessibility</h2><div className="mt-4 space-y-3 text-sm"><label className="flex items-center justify-between"><span>Reduced motion</span><input type="checkbox" /></label><label className="flex items-center justify-between"><span>High contrast</span><input type="checkbox" /></label><label className="flex items-center justify-between"><span>Keyboard hints</span><input type="checkbox" defaultChecked /></label></div></Card><Card><Bookmark className="text-mint" /><h2 className="mt-3 font-display text-2xl font-extrabold">Bookmarks</h2><p className="mt-3 text-sm text-slate-600 dark:text-slate-300">Binary Search, Big O Growth Curves, Trie Insert</p></Card><Card><Award className="text-amber" /><h2 className="mt-3 font-display text-2xl font-extrabold">Badges</h2><div className="mt-4 space-y-2">{achievements.slice(0, 4).map((item) => <p key={item} className="rounded-lg bg-slate-100 p-3 text-sm font-semibold dark:bg-white/10">{item}</p>)}</div></Card></div><Card className="mt-8"><div className="flex items-center gap-3"><CalendarDays className="text-brand" /><h2 className="font-display text-2xl font-extrabold">Daily challenge calendar</h2></div><div className="mt-4 grid grid-cols-7 gap-2">{Array.from({ length: 28 }).map((_, index) => <div key={index} className={`aspect-square rounded-md ${index % 5 === 0 ? "bg-amber" : "bg-mint/70"}`} />)}</div></Card></div></AppShell>;
}
